# Detector–policy comparison status

The four-detector pipeline is being executed. The final comparison requires 24 detector-specific controllers and matched evaluations for ADWIN, HDDM-W, Hellinger and D3 OOF.

The final report will be written here automatically after all executions and trace checks pass. Progress is recorded in `../results/detector_policy_study/`; phase-04 progress is in `training/04_rl_training/status.json`.

The completed ADWIN-only results remain in `../results/policy_robustness/`, with their original report in `../provenance/adwin_only_source/07_documentation/RESULTS.md`. They do not establish the four-detector result.
