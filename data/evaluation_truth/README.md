# Offline evaluation references

These files record original task labels, modified row positions and replay references. They are used to score saved predictions after execution and are not training inputs.

The separate protected 5% validation samples are stored under each result set's `audit/protected` folder. A protected sample is available to the accuracy check after its recorded delay; it is not the full evaluator reference file.
