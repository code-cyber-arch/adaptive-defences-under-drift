# Shared support

`protocol.py` handles paths, hashes and execution records. `channels.py` keeps learner observations, protected validation samples and offline references separate. `generation.py` makes seeds reproducible. `validation.py` checks feature schemas and lineage. These helpers support the numbered phases.
