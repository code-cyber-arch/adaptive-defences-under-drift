# Configurations

`thesis.json` defines the full experiment. `smoke.json` defines a small implementation check. The main pipeline records its training, validation and evaluation configurations under `results/configurations`. Model results are never copied across different contracts.
