# Verification tests

The tests check candidate isolation, reserved-sample exclusion, delayed RL rewards, frozen evaluation and full RADAR coverage. Run them from the project root with `python -B -m unittest discover -s tests -v`. Test realizations are not research results.
